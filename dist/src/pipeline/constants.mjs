const REPETITION_BOOST_MAX = .2;
const REPETITION_BOOST_LOG_SCALE = .08;
const SENSITIVITY_SENSITIVE_VALUE_BOOST = .15;
const SENSITIVITY_PROMPT_INJECTION_BOOST = .8;
const DEFAULT_SCORES = {
	durable_state: {
		salience: .76,
		utility: .68,
		stability: .74
	},
	session_state: {
		salience: .58,
		utility: .54,
		stability: .46
	},
	stable_fact: {
		salience: .82,
		utility: .8,
		stability: .86
	},
	graph_relation: {
		salience: .78,
		utility: .84,
		stability: .72
	},
	episodic_event: {
		salience: .62,
		utility: .52,
		stability: .36
	},
	fallback: {
		salience: .14,
		utility: .08,
		stability: .1
	}
};
const SOURCE_RELIABILITY = {
	tool: .95,
	user: .82,
	assistant: .56,
	synthesized: .68,
	default: .64
};
const FALLBACK_PRIOR_CONFIDENCE = {
	state: .7,
	task: .66,
	fact: .76,
	event: .62,
	graph_edge: .68,
	chunk: .44,
	strategy: .5
};
const FALLBACK_SOURCE_RELIABILITY = {
	chunk: .52,
	task: .72,
	strategy: .58,
	default: .7
};
const POSTERIOR_USEFULNESS_WEIGHT = .3;
const POSTERIOR_STABILITY_WEIGHT = .15;
const POSTERIOR_OUTCOME_WEIGHT = .15;
const POSTERIOR_CONSISTENCY_WEIGHT = .1;
const POSTERIOR_TEMPORAL_WEIGHT = .08;
const POSTERIOR_PROMOTION_WEIGHT = .05;
const POSTERIOR_CONTRADICTION_WEIGHT = .35;
const POSTERIOR_DEMOTION_WEIGHT = .15;
const POSTERIOR_CORRECTION_WEIGHT = .26;
const POSTERIOR_REPEATED_USE_WEIGHT = .08;
const POSTERIOR_STALE_DECAY_WEIGHT = .12;
const POSTERIOR_MIDPOINT = .5;
const SCHEDULER_DEDUP_PROBE_SCALE = .55;
const SCHEDULER_DEDUP_PROBE_FLOOR = .46;
const SCHEDULER_SUMMARY_SCORE_BOOST = 1.04;
const CHUNK_VECTOR_CONFIDENCE = .88;
const TASK_VECTOR_CONFIDENCE = .9;
const STRATEGY_MAX_CONTRADICTION = .4;
//#endregion
export { CHUNK_VECTOR_CONFIDENCE, DEFAULT_SCORES, FALLBACK_PRIOR_CONFIDENCE, FALLBACK_SOURCE_RELIABILITY, POSTERIOR_CONSISTENCY_WEIGHT, POSTERIOR_CONTRADICTION_WEIGHT, POSTERIOR_CORRECTION_WEIGHT, POSTERIOR_DEMOTION_WEIGHT, POSTERIOR_MIDPOINT, POSTERIOR_OUTCOME_WEIGHT, POSTERIOR_PROMOTION_WEIGHT, POSTERIOR_REPEATED_USE_WEIGHT, POSTERIOR_STABILITY_WEIGHT, POSTERIOR_STALE_DECAY_WEIGHT, POSTERIOR_TEMPORAL_WEIGHT, POSTERIOR_USEFULNESS_WEIGHT, REPETITION_BOOST_LOG_SCALE, REPETITION_BOOST_MAX, SCHEDULER_DEDUP_PROBE_FLOOR, SCHEDULER_DEDUP_PROBE_SCALE, SCHEDULER_SUMMARY_SCORE_BOOST, SENSITIVITY_PROMPT_INJECTION_BOOST, SENSITIVITY_SENSITIVE_VALUE_BOOST, SOURCE_RELIABILITY, STRATEGY_MAX_CONTRADICTION, TASK_VECTOR_CONFIDENCE };
